"""评估 Prompt：LLM-as-judge 对客服表现打分（第9期）。

三个 judge 对应需要「理解语义」才能判定、规则代码搞不定的维度：
- ANSWER_QUALITY_PROMPT：回答质量（结果指标）
- HALLUCINATION_PROMPT：事实核查（结果指标，第10轮扩展为四态判定）
- PROCESS_SOUNDNESS_PROMPT：过程合理性（过程指标，第10轮扩展为接收完整上下文）

均要求 temperature=0.0、只输出 JSON（JSON 示例用 {{ }} 转义以兼容 .format）。
"""

ANSWER_QUALITY_PROMPT = """你是电商客服质量评审专家。请根据用户问题和客服回复，对回复的「响应质量」打分。

【用户问题】
{user_input}

【客服回复】
{reply}

【参考要点】（可选，用于判断回复是否覆盖了应有信息，"（无）"表示不提供参考）
{reference}

评分维度：是否准确、完整、专业、语气得体，是否直接解决了用户问题。
- 5 分：准确、完整、专业、语气得体，直接解决问题，无多余或错误信息
- 4 分：基本解决问题，信息准确，但略有冗余或可改进之处
- 3 分：部分解决问题，或信息不够完整/不够清晰
- 2 分：回复偏离问题，或包含明显不准确信息
- 1 分：完全没有解决问题，答非所问，或包含严重错误

只输出 JSON，不要加 markdown 代码块：
{{
  "score": <1到5的整数>,
  "reason": "<一句话中文评分理由>"
}}"""


HALLUCINATION_PROMPT = """你是电商客服事实核查专家。客服回复中涉及订单、商品、物流、政策等具体信息时，
必须来自工具返回的真实数据，不能凭空编造。请将客服回复对照工具返回数据，判定为以下四类之一：

【客服回复】
{reply}

【本次会话的工具调用与返回数据】（这是唯一可信的事实来源；含每次调用的工具名、参数、
成功状态与返回内容。status=error 或返回 success=false 表示该次工具调用失败）
{observations}

判定标准（四类，互斥）：
- supported（有依据）：回复中的具体事实（订单状态、价格、物流节点、库存、政策条款等）
  都能在成功返回的工具数据中找到依据；或回复不涉及需要核查的具体事实（如纯问候、
  一般性表述）→ 忠实。
- contradicted（矛盾）：回复中的具体事实与工具返回数据直接矛盾。
- unsupported（无依据）：工具数据缺失、检索结果为空或工具调用失败，但回复仍然断言了
  具体事实（如具体价格、政策天数、物流进度、库存数字）→ 按无依据/编造处理，
  绝不能判为 supported。
- unverifiable（无法核实）：工具数据缺失或工具调用失败（如检索失败），且回复没有断言
  需要核实的具体事实，而是明确说明「暂时无法核实/需要进一步确认」→ 既不算有依据，
  也不算编造。

只输出 JSON，不要加 markdown 代码块：
{{
  "verdict": "supported | contradicted | unsupported | unverifiable",
  "reason": "<一句话中文核查理由，指出依据或矛盾点>"
}}"""


PROCESS_SOUNDNESS_PROMPT = """你是 Agent 推理过程评审专家。请判断客服 Agent 为解决用户问题而执行的「过程」是否合理。
你必须结合完整的对话上下文、每次工具调用的细节和标准作业流程（SOP）来评判，而不是只看工具名。

【完整用户输入】（多轮对话按时间顺序）
{user_turns}

【实际工具调用明细】（按先后顺序；包含调用发生在第几轮、工具描述、参数、返回结果与成功状态）
{tool_steps}

【相关标准作业流程 SOP】（可选，"（无）"表示没有提供）
{sop}

评分维度：工具选择是否对路、顺序是否合理、是否符合 SOP、有无明显多余、缺失、
重复或违规（如敏感操作未确认、禁止场景调用）的调用。
- 5 分：工具选择精准、顺序合理、符合 SOP、无多余调用
- 4 分：基本合理，略有可优化空间
- 3 分：能解决问题但有冗余调用或顺序欠佳
- 2 分：调用了不相关/违规工具，或缺少关键调用，明显偏离 SOP
- 1 分：过程明显错误（答非所问该调不调、大量无关调用、违反禁用约束）

只输出 JSON，不要加 markdown 代码块：
{{
  "score": <1到5的整数>,
  "reason": "<一句话中文评分理由，尽量引用具体调用或 SOP 步骤>"
}}"""
